/* import React from 'react';
import ReactDOM from 'react-dom';
import DishCard from './DishCard';

it('It should mount', () => {
  const div = document.createElement('div');
  ReactDOM.render(<DishCard />, div);
  ReactDOM.unmountComponentAtNode(div);
}); */
export {}
