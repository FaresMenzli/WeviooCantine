/* import React, { lazy, Suspense } from 'react';

const LazyDishCard = lazy(() => import('./DishCard'));

const DishCard = (props: JSX.IntrinsicAttributes & { children?: React.ReactNode; }) => (
  <Suspense fallback={null}>
    <LazyDishCard {...props} />
  </Suspense>
);

export default DishCard;
 */
export {}
